# Uygulama Ortam Değişkenleri (örnek)

# PostgreSQL bağlantısı (lokalde çalıştırırken kendi değerlerinizi yazın)
# Format: postgresql://KULLANICI:SIFRE@HOST:PORT/VERITABANI?schema=public
DATABASE_URL=postgresql://cek_admin:cek_secret_2024@localhost:5433/cek_db?schema=public

# NextAuth yapılandırması (geliştirme için örnek değerler)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=cek_super_secret_jwt_key_2024

# (Opsiyonel) Üretim için örnekler
# NEXTAUTH_URL=https://app.ornek-domin.com
# DATABASE_URL=postgresql://kullanici:sifre@db:5432/cek_db?schema=public
